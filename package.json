{
  "name": "relay-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter relay-api dev",
    "dev:agent": "python apps/translation-agent/main.py dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "pnpm --filter shared db:push",
    "db:generate": "pnpm --filter shared db:generate"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
